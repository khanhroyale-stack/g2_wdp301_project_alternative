# Core Project Information
- C2C marketplace app for renting and buying/selling items (WDP301 Project).
- Modules: `backend` and `frontend`.
- Roles: `user` (post, rent/buy, chat, review, report), `shipper` (deliver, inspect), `admin` (approve, manage users/reports).
- See `mem:tech_stack` for technology choices.
- See `mem:conventions` for coding rules and structures.
- See `mem:suggested_commands` for dev server commands.
- See `mem:task_completion` for completion steps.